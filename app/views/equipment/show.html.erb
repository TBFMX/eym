<p id="notice"><%= notice %></p>

<div class="show_equipment">
  <div class="titulo"><h2><%= @equipment.name %></h2></div>
  <div class="producto">
    <div class="preview">
      <%
      @gallery.each do |g|
      @images = Image.where('gallery_id' => g.id)
      link_to g.title, 'equipment/galleries/'+ g.equipment_id.to_s
      %>

      <table>
        <tr>
          <% @cont = 0 %>
          <% @images.each do |img| %>
          <% if @cont >= 2 %>
        </tr>
        <tr>
          <% @cont = 0 %> 
          <% end %>   
            <td><img src="<%= img.image_url %>"  height="42" width="42" > </td>
          <% @cont +=1 %> 
          <% end %>
          <% if @cont%2 == 0 %> <!-- nada --> <% else %> <td></td><%end%>
        </tr>
      </table>

    <% end %>
    </div>
    <div class="imagen"><img src="<%= @image.image_url %>"></div>
    <div class="modelo">
      <table>
        <tr><th colspan="2">Modelo: <%= @equipment.modelo %></th></tr>
        <tr><td>Año: <%= @equipment.year %></td><td>Color: <%= @equipment.color %></td></tr>
        <tr><td>Marca: <%= @equipment.brand_id %></td><td> </td></tr>
        <tr><th colspan="2">Venta / Renta</th></tr>
        <tr>
          <td>Precio Venta:</td>
          <td><%= number_to_currency(@equipment.precio) %> <%= @equipment.moneda %></td>
        </tr>
        <tr><th colspan="2">Contacto y localidad</th></tr> 
        <tr>
          <td colspan="2">Lugar: <%= @equipment.ciudad %>, <%= @equipment.estado %>, <%= @equipment.pais %></td>
        </tr>        
        <tr>
          <td><input type="button" name="Contactar" value="Contactar"></td>
          <td>(Nombre del vendedor)</td>
        </tr>                 
      </table>
    </div>
    </div>
  <p>Etiquetas: xxxx,xxxx,xxxx,xxxx,xxxx,xxxx</p>
  <div class="descripcion">
    <h1>Description:</h1>
    <p><%= @equipment.description %></p>
  </div>
</div>

<%= link_to 'Edit', edit_equipment_path(@equipment) %> |
<%= link_to 'Back', equipment_index_path %>